name: Detect change in file version

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create Results Directory and File
        run: |
          mkdir results
          echo "<html><body>" > results/results.html
          echo "<h1>Comparison Results</h1>" >> results/results.html
          reference_state=$(cat reference_state.txt)
          current_state=$(cat package.json)
          
          if [ "$reference_state" != "$current_state" ]; then
            echo "<p>File has been modified</p>" >> results/results.html
          else
            echo "<p>File has not been modified</p>" >> results/results.html
          fi
          echo "</body></html>" >> results/results.html

      - name: Display Results
        run: cat results/results.html

          
      - name: Deploy Logs to GitHub Pages
        if: steps.compare_states.outputs.compare_states == 'File has been modified' || steps.compare_states.outputs.compare_states == 'File has not been modified'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./results/
          commit_message: "Publish Compare States Logs"
