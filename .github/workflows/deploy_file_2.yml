name: Execute file2.sh and Display Output

on:
  workflow_dispatch:

jobs:
  execute:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Execute file2.sh
        run: chmod +x ./files/file2.sh && ./files/file2.sh > output.txt

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          commit_message: "Execute file2.sh and Display Output"

      - name: Display Output
        run: |
          echo "<html><body>" > output.html
          echo "<h1>Output of file2.sh</h1>" >> output.html
          echo "<pre>" >> output.html
          cat output.txt >> output.html
          echo "</pre>" >> output.html
          echo "</body></html>" >> output.html
